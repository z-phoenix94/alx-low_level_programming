#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: prints name of the program
 * @argc: arguemnt count, counts command line aruments
 * @argv: argument vector, holds the passed arguments
 *
 * Return: Always 0 (Success)
 */

int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);

	return (0);
}
