#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: prints the name of a file that
 * it compiled from
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
